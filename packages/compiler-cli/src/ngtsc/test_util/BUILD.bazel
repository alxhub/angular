package(default_visibility = ["//visibility:public"])

load("//tools:defaults.bzl", "ts_library")

ts_library(
    name = "test_util",
    srcs = glob([
        "index.ts",
        "src/**/*.ts",
    ]),
    deps = [
        "//packages:types",
    ],
    testonly = True,
)
